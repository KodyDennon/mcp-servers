# mcp-ios-simulator

## 0.2.1

### Patch Changes

- Comprehensive security fixes and improvements, updated build artifacts, and general MCP server fixes.

## 0.2.0

### Minor Changes

- feat: add the iOS Simulator MCP server with WebKit inspector tooling plus native automation via WebDriverAgent (taps, swipes, text entry, install/launch/terminate apps) so agents can fully drive and test PWAs or native builds inside the simulator. Includes an `ios-simulator-mcp setup-config` helper to auto-add the server to Claude/MCP config files and detailed environment bootstrapping.

## 0.1.0

### Minor Changes

- feat: add the iOS Simulator MCP server with WebKit inspector tooling plus native automation via WebDriverAgent (taps, swipes, text entry, install/launch/terminate apps) so agents can fully drive and test PWAs or native builds inside the simulator.
